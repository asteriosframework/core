
# Select what we should cache between builds
cache:
  key: build_cache
  paths:
    - vendor/
    - node_modules

# Run our tests
# If Xdebug was installed you can generate a coverage report and see code coverage metrics.
test:
  stage: test
  image: ${DOCKER_IMAGE}
  needs: ['build']
  services:
    - name: mariadb:10.4@sha256:22edfe1c78349f8cae88bbb5c2873b7e6c515be767c91c691308ce57445806f3
      alias: db
      variables:
        MARIADB_USER: db
        MARIADB_PASSWORD: db
        MARIADB_ROOT_PASSWORD: db
        MARIADB_DATABASE: db
  variables:
    XDEBUG_MODE: coverage
  script:
    - composer tests:ci
  artifacts:
    when: always
    reports:
      junit: phpunit-report.xml
      coverage_report:
        coverage_format: cobertura
        path: phpunit-coverage.xml
    paths:
      - coverage-report/
  coverage: '/^\s+Lines:\s+[0-9]+[.][0-9]+\%/'

code-quality:
  image: ${DOCKER_IMAGE}
  needs: ['build']
  stage: test
  script:
    - composer cs-fixer:check
    - composer phpstan
  cache:
    key: build_cache
    paths:
      - vendor/
      - node_modules/
